{
  "dependencies": {
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky",
    "lint": "pnpm --filter ./frontend lint && pnpm --filter ./backend lint",
    "lint:fix": "pnpm --filter ./frontend exec eslint --fix . && pnpm --filter ./backend exec eslint --fix ."
  }
}
